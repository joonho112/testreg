% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_stan.R
\name{meta_reg_stan}
\alias{meta_reg_stan}
\title{Fit Bayesian Meta-Regression via Stan}
\usage{
meta_reg_stan(
  yi,
  vi,
  mods = NULL,
  data,
  chains = 4,
  iter = 2000,
  warmup = 1000,
  seed = 12345,
  backend = "cmdstanr"
)
}
\arguments{
\item{yi}{Character string naming the effect-size column, or a numeric
vector of observed effect sizes.}

\item{vi}{Character string naming the sampling-variance column, or a
numeric vector of sampling variances.}

\item{mods}{A one-sided formula for moderators (e.g., \code{~ dosage + duration})
or \code{NULL} for an intercept-only model.}

\item{data}{A data frame containing the variables.}

\item{chains}{Number of MCMC chains (default 4).}

\item{iter}{Number of total iterations per chain (default 2000).}

\item{warmup}{Number of warmup iterations per chain (default 1000).}

\item{seed}{Random seed for reproducibility.}

\item{backend}{Character: \code{"cmdstanr"} (default) or \code{"rstan"}.}
}
\value{
An object of class \code{"metareg_fit"}.
}
\description{
Fits a random-effects meta-regression model using Stan (cmdstanr preferred,
rstan as fallback).
}
\examples{
\dontrun{
data(example_meta)
fit <- meta_reg_stan(yi = "yi", vi = "vi", mods = ~ dosage,
                     data = example_meta, chains = 2, iter = 1000)
summary(fit)
}
}
